# Orphaned / conflicting SQL kept out of the Drizzle migrate path
# These were never registered in meta/_journal.json (duplicate version tags or one-off repairs).
# Do not move back into lib/db/migrations without a deliberate journal entry.
