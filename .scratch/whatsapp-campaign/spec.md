# Spec — Finish click-to-WhatsApp campaigns

Status: ready-for-agent
Feature: whatsapp-campaign

See the authoritative copy in the frontend repo:
`.scratch/whatsapp-campaign/spec.md`

This backoffice branch implements Phase 4 (AI wizard objective, greeting
fields, fallback `whatsappWelcome`, copy route `whatsapp`) and the Page
number route under `app/api/meta-marketing/[accountId]/pages/[pageId]/whatsapp-number/`.
