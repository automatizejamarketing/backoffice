/** Matches `aspect-[16/9]` on checkout and in-app product cards. */
export const PRODUCT_COVER_ASPECT_RATIO = 16 / 9;

/** Output size served to customers (checkout + biblioteca). */
export const PRODUCT_COVER_OUTPUT_WIDTH = 1280;
export const PRODUCT_COVER_OUTPUT_HEIGHT = 720;
